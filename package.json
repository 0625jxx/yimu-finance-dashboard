{
  "name": "personal-finance-dashboard",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/server.js",
    "test": "node --test",
    "validate": "node --test && pwsh -File ./tests/validate-docs.ps1"
  }
}
